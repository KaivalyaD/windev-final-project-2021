using System;

public class DotNetTrigServer
{
	public float Sine(float theta)
    {
        // Code
        return (float)Math.Sin((double)theta);
    }

    public float Cosine(float theta)
    {
        // Code
        return (float)Math.Cos((double)theta);
    }
}
